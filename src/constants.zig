const build_options = @import("build_options");

pub const DEFAULT_WINDOW_WIDTH: f32 = 1920;
pub const DEFAULT_WINDOW_HEIGHT: f32 = 1080;
pub const DEFAULT_USER_WINDOW_SCALE: f32 = 1.0 / 1.2;

pub const WEB_BUILD = build_options.web_build;
